# Public API origin only (scheme + host + optional port). Never put secrets here.
# Used as the browser fetch target for the handwritten API client (credentials: include).
#
# Development: if unset, the app defaults to http://localhost:3000.
# Deployed / production builds: must be set explicitly — there is no localhost fallback.
VITE_API_ORIGIN=http://localhost:3000
