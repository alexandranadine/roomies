{
  "name": "@roomies/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared HTTP contracts and types for Roomies (no domain logic)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
