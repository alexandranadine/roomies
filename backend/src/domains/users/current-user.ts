/**
 * Canonical Roomies User as exposed by the authenticated product surface.
 * Profile, Home, Membership, and AuthIdentity fields are out of scope.
 */
export type CurrentUser = {
  id: string;
};
